output: dist/
releases:
  - name: production
    jobs:
      - name: release-linux-appimage
        package:
          platform: linux
          target: appimage
      # - name: release-android
      #   package:
      #     platform: android
      #     target: apk
